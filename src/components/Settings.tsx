import React from 'react';

const Settings = () => {
  const double = 'Double';

  const single = 'single';

  return (
    <div>
      Settings{double}
      {single}
    </div>
  );
};

export default Settings;
